<section id="add">
  <form action="/" method="post">
    <textarea name="content" placeholder="Not'un&hellip;"></textarea>
    <input type="submit" value="Not Al!">
  </form>
</section>
 
<% # display notes %>

<% @notes.each do |note| %>
  <article <%= 'class="complete"' if note.complete %>>
    <p>
      <%= note.content %>
      <span><a href="/<%= note.id %>">[edit]</a></span>
    </p>
    <p class="links">
      <a href="/<%= note.id %>/complete">&#8623;</a>
    </p>
    <p class="meta">Oluşturuldu: <%= note.created_at %></p>
  </article>
<% end %>